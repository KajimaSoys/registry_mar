from .views import *
from django.urls import path, include


urlpatterns = [
    path("get_schedule' }}", get_schedule),
    path("get_medcard' }}", get_medcard),
    path("get_record' }}", get_record),
    path("get_cost' }}", get_cost),
    path("change_schedule' }}", change_schedule),
]